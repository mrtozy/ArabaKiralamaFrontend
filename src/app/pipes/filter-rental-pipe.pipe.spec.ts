import { FilterRentalPipePipe } from './filter-rental-pipe.pipe';

describe('FilterRentalPipePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterRentalPipePipe();
    expect(pipe).toBeTruthy();
  });
});
